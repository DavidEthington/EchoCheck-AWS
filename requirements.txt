# --- Core AWS & Security ---
boto3>=1.34.0          # AWS SDK for Python
botocore>=1.34.0       # Low-level AWS core (needed for Error Handling)
python-dotenv>=1.0.0   # For loading .env files during local testing

# --- UI & Data Visualization ---
streamlit>=1.31.0      # The Dashboard Framework
pandas>=2.1.0          # Dataframe handling for those "nxc" tables
watchdog>=4.0.0        # Performance optimization for Streamlit in dev mode

# --- Concurrency & Performance ---
futures>=3.1.1         # Backport for advanced thread pool management

# --- Testing & Validation (The "Safe" Way) ---
moto[iam,sts,s3]>=5.0.0  # For mocking AWS services to test without live keys
pytest>=8.0.0            # The industry standard for running test suites
